{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/subspeciesprofiler/master/assets/schema_input.json",
    "title": "nf-core/subspeciesprofiler pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "species": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Species name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "genomes_dir": {
                "type": "string",
                "format": "directory-path",
                "exists": true,
                "errorMessage": "Path to the directory containing the gzipped genomes must be provided and must exist"
            },
            "qc_tsv": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.tsv$",
                "errorMessage": "Path to the completeness/contamination TSV must be provided, must exist and have extension '.tsv'"
            }
        },
        "required": ["species", "genomes_dir", "qc_tsv"]
    },
    "uniqueEntries": ["species"]
}
