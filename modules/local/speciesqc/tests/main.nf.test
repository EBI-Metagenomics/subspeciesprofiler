nextflow_process {

    name "Test Process SPECIESQC"
    script "../main.nf"
    process "SPECIESQC"

    tag "modules"
    tag "modules_local"
    tag "speciesqc"

    test("species qc - csv") {

        when {
            process {
                """
                input[0] = [
                    [ id:'E_coli' ],
                    file("${projectDir}/tests/fixtures/E_coli_qc.csv", checkIfExists: true),
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(
                    process.out.report,
                    process.out.rfile,
                    process.out.labels
                ).match() }
            )
        }

    }

    test("species qc - csv - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [
                    [ id:'E_coli' ],
                    file("${projectDir}/tests/fixtures/E_coli_qc.csv", checkIfExists: true),
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(
                    process.out.report,
                    process.out.rfile,
                    process.out.labels
                ).match() }
            )
        }

    }

}
